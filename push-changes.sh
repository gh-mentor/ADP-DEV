# this script uses git commands to stage, commit, and push to the remote repository

# Stage all changes
git add .

# Commit changes
git commit -m "Update"

# Push changes to remote repository targeting the 'main' branch
git push origin main

# Exit script
